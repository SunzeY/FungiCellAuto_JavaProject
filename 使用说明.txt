1.运行.exe
2.输入开创自动机的size
3.在GUI中运行程序：
update：根据输入表格中的数据随机生成初始状态（名称必须为"A""B""C"..., 第一个参数为没刷新一次吃掉底物的速度（1-20），第二个参数为生长速度（5-20）， 第三个参数没用上）
recode：将从开始运行当前状态的数据写入data_out.txt
single：单步运行
run：从现在的状态运行200步（自动记录数据）

